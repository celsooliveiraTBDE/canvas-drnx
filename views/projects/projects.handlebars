<link rel="stylesheet" href="/assets/css/trips.css">
{{> nav }}
{{> login-modal }}
{{> new-project-modal }}
<div class="all-trips-container container">
	
	<div class="row">
		{{#if project }}
		{{#each project}}
		<div class="project-container col-xs-12 col-sm-6 col-lg-6">
			<div class="project-subcontainer project-default">
				<h3 class="lead">
					Trip: {{ this.dataValues.id }} Test project name
				</h3>
				<div class="bottom-project-container">
					<div class="risk-analysis-container">
							{{!-- {{ riskAnalysis }} --}}
						<p class="risk-analysis">
							Some Kind of Risk Analysis
						</p>
					</div>
					<div class="destination-container">
							{{!-- {{ riskAnalysis }} --}}
						<p class="destination">
							{{ this.dataValues.destination }}
						</p>
					</div>
					<button type="button" class="btn btn-circle"><i class="">10</i></button>
				</div>
			</div>
		</div>
		{{/each}}
		{{/if}}
	<button id="add-project-button" data-toggle="modal" data-target="#squarespaceModal" type="button" class="btn btn-circle"><i class="glyphicon glyphicon-plus"></i></button>
	</div>
</div>